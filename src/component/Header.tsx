import React from 'react';

const Header : React.FC = () => {
    return (
        <header className="header" style={{marginBottom:"30px"}}>
 		    <h1> <a href="#">Cricketers'<span>Club</span></a></h1>
        </header>
  
    );
};

export default Header;